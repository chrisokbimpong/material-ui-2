import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#574b90",
      light: "skyblue",
    },
    secondary: {
      main: "#880061",
    },
    otherColor: {
      main: "#999",
    },
  },
});
